# Image evaluation cases

These text-only cases were written for HyperGen and may be reused with the
project. Each case fixes a Description, selected stack Style, reference render
prompt, required visual elements, and unwanted artifacts. Generated images
belong under the ignored `evals/runs/` directory.
